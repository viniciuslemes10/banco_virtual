spring.application.name=VLbank

# Database Settings
spring.datasource.url=jdbc:mysql://localhost:3306/vlbank?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=viniciuslemes10

# Configuration for automatic schema management
spring.jpa.hibernate.ddl-auto=update

# Display SQL queries in the console
spring.jpa.show-sql=true

# Format SQL queries
spring.jpa.properties.hibernate.format_sql=true